(() => {
  'use strict';
  if (window.__IECG_D1_V3__) return;
  window.__IECG_D1_V3__ = true;

  const ready = fn => document.readyState === 'loading' ? document.addEventListener('DOMContentLoaded', fn, {once:true}) : fn();
  const esc = v => String(v ?? '').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money = v => new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v)||0);
  const dateBR = v => { if(!v)return '-'; const d=new Date(`${v}T00:00:00`); return Number.isNaN(d.getTime())?esc(v):d.toLocaleDateString('pt-BR'); };
  const toast = (m,k='info') => window.iecgToast ? window.iecgToast(m,k==='error'?'Atenção':k==='ok'?'Tudo certo':'Portal Administrativo') : alert(m);
  const api = async (path,options={}) => { const r=await fetch(path,{credentials:'same-origin',...options,headers:{'Content-Type':'application/json',...(options.headers||{})}}); let d=null; try{d=await r.json()}catch(_){} if(!r.ok)throw new Error(d?.error||`Falha HTTP ${r.status}`); return d; };
  const field=(label,id,type='text',placeholder='',value='')=>`<div class="field"><label for="${id}">${label}</label><input id="${id}" type="${type}" placeholder="${placeholder}" value="${esc(value)}"></div>`;
  const select=(label,id,options,selected='')=>`<div class="field"><label for="${id}">${label}</label><select id="${id}">${options.map(o=>`<option value="${esc(o.value)}" ${String(o.value)===String(selected)?'selected':''}>${esc(o.label)}</option>`).join('')}</select></div>`;
  const openModal=(title,body,save,saveLabel='Salvar')=>{if(typeof window.abrirModal!=='function')return toast('A janela de cadastro não está disponível nesta tela.','error');window.abrirModal(title,body+`<div class="modal-foot"><button type="button" class="btn secondary" id="d1Cancel">Cancelar</button><button type="button" class="btn primary" id="d1Save">${saveLabel}</button></div>`);document.getElementById('d1Cancel')?.addEventListener('click',()=>window.fecharModal?.());document.getElementById('d1Save')?.addEventListener('click',save);};
  const table=(heads,rows)=>rows.length?`<div class="table-wrap"><table class="table"><thead><tr>${heads.map(h=>`<th>${h}</th>`).join('')}</tr></thead><tbody>${rows.join('')}</tbody></table></div>`:'<div class="empty"><b>Nenhum registro encontrado</b><span>Use o botão de adicionar para começar.</span></div>';
  const badge=(status)=>{const key=String(status||'').toLowerCase();const kind=['ativo','ativa','pago','concluida'].includes(key)?'ok':['atrasado','cancelado','inativo','desligado'].includes(key)?'danger':'warn';return `<span class="badge ${kind}">${esc(status||'-')}</span>`;};
  const set=(id,html)=>{const e=document.getElementById(id);if(e)e.innerHTML=html;};
  const role=()=>window.__IECG_ROLE__||'administrador';

  function setHeader(user){
    window.__IECG_USER_ID__=user.id; document.querySelectorAll('#usuarioAtual,.user-chip,.user').forEach(e=>e.textContent=user.full_name||'Acesso'); document.querySelectorAll('#sideUser').forEach(e=>e.textContent=user.full_name||'Acesso'); document.querySelectorAll('#sideRole,#configRole').forEach(e=>e.textContent=user.role||'Perfil'); document.querySelectorAll('.live,.status').forEach(e=>{e.textContent='● Banco D1 conectado';e.title='Dados sincronizados com o banco D1.'});
  }
  function buildRoleNav(user){
    const nav=document.getElementById('nav'); if(!nav)return; const r=user.role||'administrador'; window.__IECG_ROLE__=r;
    const links=r==='administrador'?[['dashboard','🏠','Dashboard'],['alunos','👨‍🎓','Alunos'],['matriculas','📋','Matrículas'],['financeiro','💰','Financeiro'],['funcionarios','👥','Funcionários'],['professores','👩‍🏫','Professores'],['cursos','📚','Cursos livres'],['usuarios','🔐','Usuários'],['config','⚙️','Configurações']]:r==='professor'?[['dashboard','🏠','Dashboard'],['profArea','👩‍🏫','Minha área'],['cursos','📚','Cursos']]:[['dashboard','🏠','Dashboard'],['alunoArea','🎓','Minha área']];
    nav.innerHTML=links.map(([id,ic,label])=>`<button type="button" data-page="${id}" title="${esc(label)}"><span class="ic">${ic}</span>${esc(label)}</button>`).join(''); nav.querySelectorAll('button').forEach(b=>b.addEventListener('click',()=>window.abrirPagina?.(b.dataset.page)));
  }
  async function render(){
    const me=await api('/api/auth/me').catch(()=>null); if(!me?.user)return; setHeader(me.user); buildRoleNav(me.user);
    if(role()!=='administrador'){try{const d=await api('/api/dashboard');set('mAlunos',d.counts.students);set('mMatriculas',d.counts.enrollments);set('mFinanceiro',money(d.counts.revenue));set('mFuncionarios',d.counts.employees);return;}catch(e){toast(e.message,'error');return;}}
    try{
      const [d,s,e,f,emp,teach,courses]=await Promise.all([api('/api/dashboard'),api('/api/students'),api('/api/enrollments'),api('/api/financial'),api('/api/employees'),api('/api/teachers'),api('/api/courses')]);
      set('mAlunos',d.counts.students);set('mMatriculas',d.counts.enrollments);set('mFinanceiro',money(d.counts.revenue));set('mFuncionarios',d.counts.employees);
      const fl=f.results||[], total=fl.reduce((n,x)=>n+Number(x.final_amount||0),0), paid=fl.reduce((n,x)=>n+Number(x.paid_amount||0),0), pending=fl.filter(x=>!['pago','cancelado'].includes(x.status)).reduce((n,x)=>n+Math.max(0,Number(x.final_amount||0)-Number(x.paid_amount||0)),0);
      set('kpiLancado',money(total));set('kpiPago',money(paid));set('kpiPendente',money(pending));
      set('alunosTable',table(['Nome','CPF','Telefone','Status','Ações'],(s.results||[]).map(a=>`<tr><td><b>${esc(a.full_name)}</b></td><td>${esc(a.cpf||'-')}</td><td>${esc(a.phone||'-')}</td><td>${badge(a.status)}</td><td><div class="actions"><button class="btn ghost" data-edit="students" data-id="${a.id}">Editar</button><button class="btn secondary" data-del="students" data-id="${a.id}">Excluir</button></div></td></tr>`)));
      set('matriculasTable',table(['Aluno','Curso','Matrícula','Mensalidade','Parcelas','Total','Status','Ações'],(e.results||[]).map(a=>`<tr><td>${esc(a.student_name)}</td><td>${esc(a.course_name)}</td><td>${esc(a.enrollment_number||'-')}</td><td>${a.monthly_amount==null?'-':money(a.monthly_amount)}</td><td>${esc(a.installment_count||'-')}</td><td>${a.total_course_amount==null?'-':money(a.total_course_amount)}</td><td>${badge(a.status)}</td><td><button class="btn secondary" data-del="enrollments" data-id="${a.id}">Excluir</button></td></tr>`)));
      set('financeiroTable',table(['Aluno','Descrição','Valor final','Pago','Vencimento','Status','Ações'],fl.map(a=>`<tr><td>${esc(a.student_name||'-')}</td><td>${esc(a.description)}</td><td>${money(a.final_amount)}</td><td>${money(a.paid_amount)}</td><td>${dateBR(a.due_date)}</td><td>${badge(a.status)}</td><td><div class="actions"><button class="btn ghost" data-edit="financial" data-id="${a.id}">Editar</button><button class="btn secondary" data-del="financial" data-id="${a.id}">Excluir</button></div></td></tr>`)));
      set('funcionariosTable',table(['Nome','CPF','Função','Telefone','Status','Ações'],(emp.results||[]).map(a=>`<tr><td><b>${esc(a.full_name)}</b></td><td>${esc(a.cpf||'-')}</td><td>${esc(a.job_title||'-')}</td><td>${esc(a.phone||'-')}</td><td>${badge(a.status)}</td><td><div class="actions"><button class="btn ghost" data-edit="employees" data-id="${a.id}">Editar</button><button class="btn secondary" data-del="employees" data-id="${a.id}">Excluir</button></div></td></tr>`)));
      set('professoresTable',table(['Nome','Registro','Telefone','Status','Ações'],(teach.results||[]).map(a=>`<tr><td><b>${esc(a.full_name)}</b></td><td>${esc(a.professional_registration||'-')}</td><td>${esc(a.phone||'-')}</td><td>${badge(a.status)}</td><td><div class="actions"><button class="btn ghost" data-edit="teachers" data-id="${a.id}">Editar</button><button class="btn secondary" data-del="teachers" data-id="${a.id}">Excluir</button></div></td></tr>`)));
      set('cursosTable',table(['Curso','Modalidade','Duração','Status','Ações'],(courses.results||[]).map(a=>`<tr><td><b>${esc(a.name)}</b></td><td>${esc(a.modality||'-')}</td><td>${esc(a.duration||'-')}</td><td>${badge(a.status)}</td><td><div class="actions"><button class="btn ghost" data-edit="courses" data-id="${a.id}">Editar</button><button class="btn secondary" data-del="courses" data-id="${a.id}">Excluir</button></div></td></tr>`)));
      try{const users=await api('/api/users');set('usuariosTable',table(['Nome','Login','Perfil','Status','Ações'],(users.results||[]).map(a=>`<tr><td><b>${esc(a.full_name)}</b></td><td>${esc(a.username||a.email||'-')}</td><td>${esc(a.role)}</td><td>${a.active?badge('Ativo'):badge('Inativo')}</td><td>${a.id===window.__IECG_USER_ID__?'<span class="badge ok">Sessão atual</span>':`<button class="btn secondary" data-toggle-user="${a.id}" data-active="${a.active}">${a.active?'Desativar':'Ativar'}</button>`}</td></tr>`)))}catch(e){console.warn('Usuários:',e);}
    }catch(e){console.error(e);toast(e.message,'error');}
  }

  async function entityData(type){const endpoint={students:'students',teachers:'teachers',employees:'employees',courses:'courses',financial:'financial'}[type];if(!endpoint)throw new Error('Tipo inválido.');const r=await api(`/api/${endpoint}`);return(r.results||[]);}
  async function editEntity(type,id){try{const rows=await entityData(type),item=rows.find(x=>Number(x.id)===Number(id));if(!item)return toast('Registro não encontrado.','error');if(type==='students')openStudent(item);if(type==='teachers')openTeacher(item);if(type==='employees')openEmployee(item);if(type==='courses')openCourse(item);if(type==='financial')openFinancial(item);}catch(e){toast(e.message,'error');}}
  function openStudent(item={}){openModal(item.id?'Editar aluno':'Cadastrar aluno',`<div class="form-grid">${field('Nome completo','fNome','text','Nome do aluno',item.full_name)}${field('CPF','fCpf','text','000.000.000-00',item.cpf)}${field('Telefone','fTel','text','(44) 99999-9999',item.phone)}${field('E-mail','fEmail','email','email@exemplo.com',item.email)}${field('Data de nascimento','fBirth','date','',item.birth_date)}<div class="field full"><label>Endereço</label><input id="fAddress" value="${esc(item.address||'')}"></div>${select('Status','fStatus',[{value:'ativo',label:'Ativo'},{value:'inativo',label:'Inativo'},{value:'trancado',label:'Trancado'},{value:'concluido',label:'Concluído'}],item.status||'ativo')}<div class="field full"><label>Observações</label><textarea id="fNotes">${esc(item.notes||'')}</textarea></div></div>`,async()=>{try{const p={full_name:fNome.value.trim(),cpf:fCpf.value.trim(),phone:fTel.value.trim(),email:fEmail.value.trim(),birth_date:fBirth.value||null,address:fAddress.value.trim(),status:fStatus.value,notes:fNotes.value.trim()};if(!p.full_name)throw new Error('Informe o nome do aluno.');await api(item.id?`/api/students/${item.id}`:'/api/students',{method:item.id?'PATCH':'POST',body:JSON.stringify(p)});fecharModal?.();toast(item.id?'Aluno atualizado.':'Aluno cadastrado.','ok');await render();}catch(e){toast(e.message,'error');}});}
  function openTeacher(item={}){openModal(item.id?'Editar professor':'Cadastrar professor',`<div class="form-grid">${field('Nome completo','proNome','text','Nome do professor',item.full_name)}${field('CPF','proCpf','text','000.000.000-00',item.cpf)}${field('Registro profissional','proReg','text','Registro',item.professional_registration)}${field('Telefone','proTel','text','(44) 99999-9999',item.phone)}${field('E-mail','proEmail','email','email@exemplo.com',item.email)}${select('Status','proStatus',[{value:'ativo',label:'Ativo'},{value:'inativo',label:'Inativo'}],item.status||'ativo')}<div class="field full"><label>Observações</label><textarea id="proNotes">${esc(item.notes||'')}</textarea></div></div>`,async()=>{try{const p={full_name:proNome.value.trim(),cpf:proCpf.value.trim(),professional_registration:proReg.value.trim(),phone:proTel.value.trim(),email:proEmail.value.trim(),status:proStatus.value,notes:proNotes.value.trim()};if(!p.full_name)throw new Error('Informe o nome.');await api(item.id?`/api/teachers/${item.id}`:'/api/teachers',{method:item.id?'PATCH':'POST',body:JSON.stringify(p)});fecharModal?.();toast(item.id?'Professor atualizado.':'Professor cadastrado.','ok');await render();}catch(e){toast(e.message,'error');}});}
  function openEmployee(item={}){openModal(item.id?'Editar funcionário':'Cadastrar funcionário',`<div class="form-grid">${field('Nome completo','funNome','text','Nome do funcionário',item.full_name)}${field('CPF','funCpf','text','000.000.000-00',item.cpf)}${field('Data de nascimento','funBirth','date','',item.birth_date)}${field('Telefone','funTel','text','(44) 99999-9999',item.phone)}${field('E-mail','funEmail','email','email@exemplo.com',item.email)}${field('Função','funJob','text','Função',item.job_title)}${field('Data de admissão','funHire','date','',item.hire_date)}${field('Salário','funSalary','number','0,00',item.salary)}${select('Forma de pagamento','funPayment',[{value:'pix',label:'PIX'},{value:'transferencia',label:'Transferência'},{value:'dinheiro',label:'Dinheiro'},{value:'outro',label:'Outro'}],item.payment_method||'pix')}${select('Status','funStatus',[{value:'ativo',label:'Ativo'},{value:'inativo',label:'Inativo'},{value:'afastado',label:'Afastado'},{value:'desligado',label:'Desligado'}],item.status||'ativo')}<div class="field full"><label>Endereço</label><input id="funAddress" value="${esc(item.address||'')}"></div><div class="field full"><label>Observações</label><textarea id="funNotes">${esc(item.notes||'')}</textarea></div></div>`,async()=>{try{const p={full_name:funNome.value.trim(),cpf:funCpf.value.trim(),birth_date:funBirth.value||null,phone:funTel.value.trim(),email:funEmail.value.trim(),job_title:funJob.value.trim(),hire_date:funHire.value||null,salary:funSalary.value?Number(funSalary.value):null,payment_method:funPayment.value,status:funStatus.value,address:funAddress.value.trim(),notes:funNotes.value.trim()};if(!p.full_name)throw new Error('Informe o nome.');await api(item.id?`/api/employees/${item.id}`:'/api/employees',{method:item.id?'PATCH':'POST',body:JSON.stringify(p)});fecharModal?.();toast(item.id?'Funcionário atualizado.':'Funcionário cadastrado.','ok');await render();}catch(e){toast(e.message,'error');}});}
  function openCourse(item={}){openModal(item.id?'Editar curso':'Cadastrar curso',`<div class="form-grid">${field('Nome do curso','curNome','text','Nome do curso',item.name)}<div class="field"><label>Modalidade</label><select id="curMod"><option value="Online">Online</option><option value="Semipresencial">Semipresencial</option><option value="Presencial">Presencial</option></select></div>${field('Duração / carga horária','curInfo','text','Ex.: 40 horas',item.duration)}${select('Status','curStatus',[{value:'ativo',label:'Ativo'},{value:'inativo',label:'Inativo'}],item.status||'ativo')}<div class="field full"><label>Descrição</label><textarea id="curDesc">${esc(item.description||'')}</textarea></div></div>`,async()=>{try{const p={name:curNome.value.trim(),modality:curMod.value,duration:curInfo.value.trim(),status:curStatus.value,description:curDesc.value.trim()};if(!p.name)throw new Error('Informe o nome.');await api(item.id?`/api/courses/${item.id}`:'/api/courses',{method:item.id?'PATCH':'POST',body:JSON.stringify(p)});fecharModal?.();toast(item.id?'Curso atualizado.':'Curso cadastrado.','ok');await render();}catch(e){toast(e.message,'error');}});const m=document.getElementById('curMod');if(m&&item.modality)m.value=item.modality;}
  async function openEnrollment(item={}){try{const[s,c]=await Promise.all([api('/api/students'),api('/api/courses')]);if(!(s.results||[]).length)throw new Error('Cadastre um aluno antes da matrícula.');if(!(c.results||[]).length)throw new Error('Cadastre um curso antes da matrícula.');openModal(item.id?'Editar matrícula':'Nova matrícula',`<div class="form-grid"><div class="field full"><label>Aluno</label><select id="mAlunoId">${s.results.map(x=>`<option value="${x.id}" ${Number(item.student_id)===Number(x.id)?'selected':''}>${esc(x.full_name)}</option>`).join('')}</select></div><div class="field full"><label>Curso</label><select id="mCursoId">${c.results.map(x=>`<option value="${x.id}" ${Number(item.course_id)===Number(x.id)?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div>${field('Número da matrícula','mNumero','text','Ex.: 2026-001',item.enrollment_number)}${field('Data de início','mInicio','date','',item.start_date)}${field('Data de término','mFim','date','',item.end_date)}${field('Valor da mensalidade','mMensal','number','0,00',item.monthly_amount)}${field('Valor total do curso','mTotal','number','0,00',item.total_course_amount)}${field('Total de mensalidades','mQtd','number','Ex.: 24',item.installment_count)}${field('Desconto (%)','mDesc','number','0',item.discount_percent)}${select('Status','mStatus',[{value:'pre_cadastro',label:'Pré-cadastro'},{value:'pendente',label:'Pendente'},{value:'ativa',label:'Ativa'},{value:'trancada',label:'Trancada'},{value:'concluida',label:'Concluída'},{value:'cancelada',label:'Cancelada'}],item.status||'ativa')}<div class="field full"><label>Observações</label><textarea id="mNotes">${esc(item.notes||'')}</textarea></div></div>`,async()=>{try{const p={student_id:Number(mAlunoId.value),course_id:Number(mCursoId.value),enrollment_number:mNumero.value.trim()||null,start_date:mInicio.value||null,end_date:mFim.value||null,monthly_amount:mMensal.value?Number(mMensal.value):null,total_course_amount:mTotal.value?Number(mTotal.value):null,installment_count:mQtd.value?Number(mQtd.value):null,discount_percent:mDesc.value?Number(mDesc.value):null,status:mStatus.value,notes:mNotes.value.trim()};await api(item.id?`/api/enrollments/${item.id}`:'/api/enrollments',{method:item.id?'PATCH':'POST',body:JSON.stringify(p)});fecharModal?.();toast(item.id?'Matrícula atualizada.':'Matrícula cadastrada.','ok');await render();}catch(e){toast(e.message,'error');}});}catch(e){toast(e.message,'error');}}
  async function openFinancial(item={}){try{const[s,e]=await Promise.all([api('/api/students'),api('/api/enrollments')]);if(!(s.results||[]).length)throw new Error('Cadastre um aluno antes do financeiro.');openModal(item.id?'Editar lançamento financeiro':'Lançamento financeiro',`<div class="form-grid"><div class="field"><label>Aluno</label><select id="finAluno">${s.results.map(x=>`<option value="${x.id}" ${Number(item.student_id)===Number(x.id)?'selected':''}>${esc(x.full_name)}</option>`).join('')}</select></div><div class="field"><label>Matrícula</label><select id="finEnroll"><option value="">Sem vínculo</option>${(e.results||[]).map(x=>`<option value="${x.id}" ${Number(item.enrollment_id)===Number(x.id)?'selected':''}>${esc(x.enrollment_number||('#'+x.id))} — ${esc(x.course_name)}</option>`).join('')}</select></div>${field('Referência','finRef','text','Ex.: 09/2026',item.reference)}${field('Descrição','finDesc','text','Mensalidade, matrícula...',item.description)}${field('Valor','finAmount','number','0,00',item.amount)}${field('Desconto','finDiscount','number','0,00',item.discount)}${field('Valor pago','finPaid','number','0,00',item.paid_amount)}${field('Vencimento','finDue','date','',item.due_date)}${select('Status','finStat',[{value:'pendente',label:'Pendente'},{value:'parcial',label:'Parcial'},{value:'pago',label:'Pago'},{value:'atrasado',label:'Atrasado'},{value:'cancelado',label:'Cancelado'}],item.status||'pendente')}<div class="field full"><label>Observações</label><textarea id="finNotes">${esc(item.notes||'')}</textarea></div></div>`,async()=>{try{const p={student_id:finAluno.value?Number(finAluno.value):null,enrollment_id:finEnroll.value?Number(finEnroll.value):null,reference:finRef.value.trim()||null,description:finDesc.value.trim(),amount:Math.max(0,Number(finAmount.value||0)),discount:Math.max(0,Number(finDiscount.value||0)),paid_amount:Math.max(0,Number(finPaid.value||0)),due_date:finDue.value||null,status:finStat.value,paid_at:finStat.value==='pago'?new Date().toISOString():null,notes:finNotes.value.trim()};if(!p.description)throw new Error('Informe a descrição.');await api(item.id?`/api/financial/${item.id}`:'/api/financial',{method:item.id?'PATCH':'POST',body:JSON.stringify(p)});fecharModal?.();toast(item.id?'Lançamento atualizado.':'Lançamento criado.','ok');await render();}catch(e){toast(e.message,'error');}});}catch(e){toast(e.message,'error');}}
  function openUser(){openModal('Criar usuário',`<div class="form-grid">${field('Nome completo','nuNome')}${field('Usuário','nuLogin')}${field('E-mail','nuEmail','email')}${field('Senha inicial','nuPass','password','Mínimo de 8 caracteres')}${select('Perfil','nuRole',[{value:'administrador',label:'Administrador'},{value:'professor',label:'Professor'},{value:'aluno',label:'Aluno'}],'aluno')}</div>`,async()=>{try{await api('/api/users',{method:'POST',body:JSON.stringify({full_name:nuNome.value.trim(),username:nuLogin.value.trim(),email:nuEmail.value.trim()||null,password:nuPass.value,role:nuRole.value})});fecharModal?.();toast('Usuário criado com sucesso.','ok');await render();}catch(e){toast(e.message,'error');}});}

  ready(()=>{
    window.criarPrimeiroAdmin=async()=>{try{await api('/api/auth/setup',{method:'POST',body:JSON.stringify({full_name:document.getElementById('setupName')?.value.trim(),username:document.getElementById('setupUser')?.value.trim(),password:document.getElementById('setupPass')?.value,password_confirmation:document.getElementById('setupPass2')?.value})});toast('Administrador criado. Faça o login.','ok');location.reload();}catch(e){toast(e.message,'error');}};
    window.entrar=async()=>{try{const login=document.getElementById('loginUser')?.value.trim(),password=document.getElementById('loginPass')?.value||'';if(!login||!password)throw new Error('Informe usuário e senha.');const result=await api('/api/auth/login',{method:'POST',body:JSON.stringify({login,password})});window.__IECG_USER_ID__=result.user?.id;document.getElementById('login')?.classList.add('hidden');document.getElementById('setup')?.classList.add('hidden');document.getElementById('app')?.classList.remove('hidden');await render();toast('Login realizado com sucesso.','ok');}catch(e){toast(e.message,'error');}};
    window.sair=async()=>{try{await api('/api/auth/logout',{method:'POST',body:'{}'});}catch(_){}location.reload();};
    window.abrirAluno=()=>openStudent();window.abrirProfessor=()=>openTeacher();window.abrirFuncionario=()=>openEmployee();window.abrirCurso=()=>openCourse();window.abrirMatricula=()=>openEnrollment();window.abrirFinanceiro=()=>openFinancial();window.abrirUsuario=()=>{if(role()!=='administrador')return toast('Somente o administrador pode criar usuários.','error');openUser();};
    document.addEventListener('click',async e=>{const edit=e.target.closest('[data-edit]');if(edit)return editEntity(edit.dataset.edit,edit.dataset.id);const del=e.target.closest('[data-del]');if(del){if(!confirm('Excluir este registro?'))return;try{await api(`/api/${del.dataset.del}/${del.dataset.id}`,{method:'DELETE'});toast('Registro excluído.','ok');await render();}catch(err){toast(err.message,'error');}}const toggle=e.target.closest('[data-toggle-user]');if(toggle){try{await api(`/api/users/${toggle.dataset.toggleUser}`,{method:'PATCH',body:JSON.stringify({active:Number(toggle.dataset.active)?0:1})});await render();}catch(err){toast(err.message,'error');}}});
    async function boot(){try{const health=await api('/api/health');if(health?.database!=='connected')return;document.documentElement.classList.add('iecg-d1-ready');const status=await api('/api/auth/status');document.getElementById('app')?.classList.add('hidden');if(status.setup_required){document.getElementById('login')?.classList.add('hidden');document.getElementById('setup')?.classList.remove('hidden');return;}const me=await api('/api/auth/me').catch(()=>null);if(me?.user){document.getElementById('setup')?.classList.add('hidden');document.getElementById('login')?.classList.add('hidden');document.getElementById('app')?.classList.remove('hidden');await render();}else{document.getElementById('setup')?.classList.add('hidden');document.getElementById('login')?.classList.remove('hidden');}}catch(e){console.warn('D1 indisponível:',e);}}
    window.addEventListener('focus',()=>document.documentElement.classList.contains('iecg-d1-ready')&&render());
    boot();
  });
})();
